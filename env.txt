//Recomended
//create a file named env.js to keep environment variables during dev
//exporte them and import them in the js files

//Remenber before production you must have evolved the proyect to a .env or env.sh file
//Then on deployement remenber to config vars if env.file is not allowed.